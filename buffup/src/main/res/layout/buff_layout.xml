<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/buffMainContent"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="start|bottom"
    android:layout_margin="10dp"
    android:visibility="gone"
    tools:visibility="visible">

    <com.buffup.views.BuffSenderView
        android:id="@+id/senderLayoutContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/btCloseBuff"
        android:layout_width="@dimen/send_icon_size"
        android:layout_height="@dimen/send_icon_size"
        android:background="?selectableItemBackground"
        android:src="@drawable/ic_btn_close"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.buffup.views.BuffQuestionView
        android:id="@+id/questionLayoutContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/senderLayoutContainer" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/answersList"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/questionLayoutContainer"
        tools:itemCount="5"
        tools:listitem="@layout/buff_answer_layout" />

</androidx.constraintlayout.widget.ConstraintLayout>